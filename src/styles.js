import {
    StyleSheet,
} from 'react-native';

const styles = StyleSheet.create({
    textInput: {
        height: 40,
    },
    input: {
        margin: 5
    },
    titleText: {
        fontSize: 40,
        fontWeight: 'bold',
        width: '100%',
        marginTop: 20,
        marginBottom: 20,
        textAlign: 'center'
    },
    ghostText: {
        fontSize: 15,
        fontWeight: 'bold',
        color: 'grey'
    },
    infoText: {},
    miniTitle: {
        fontSize: 15,
        fontWeight: 'bold',
    }
});

export default styles;
